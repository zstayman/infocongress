<h1 class="welcome">WELCOME TO INFOCONGRESS</h1>
<% unless logged_in? %>
<span class="links">
<h4 class="link-button"><%= link_to("Create an Account", new_user_path) %></h4>
</span>
<span class="links">
<h4 class="link-button"><%= link_to("Log In", "/login") %></h4>
</span>
  <% end %>